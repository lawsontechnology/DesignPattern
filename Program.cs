// See https://aka.ms/new-console-template for more information
using DesignPattern;

// Console.WriteLine("Hello, World!");

// Singleton fromTeachaer = Singleton.GetInstance;
// fromTeachaer.PrintDetails("From Teacher");
// Singleton fromStudent = Singleton.GetInstance;
// fromStudent.PrintDetails("From Student");

// Singleton fromParent = new Singleton();
// fromParent.PrintDetails("from Parent");

      // Abstract Factory Design Pattern
//  IVehicleFactory regularVehicleFactory = new RegularCarFactory();
//  var regularCar = regularVehicleFactory.CreateCar();
//  regularCar.GetDetails();

//    IVehicleFactory sportsVehicleFactory = new SportCarFactory();
//    ICar sportCar = sportsVehicleFactory.CreateCar();
//    sportCar.GetDetails();

                  // factoryMethod designPattern
      //      FactoryMethods creditCard = new MoneyBackFactory().CreateProduct();
      //       if (creditCard != null)
      //       {
      //           Console.WriteLine("Card Type : " + creditCard.GetCardType());
      //           Console.WriteLine("Credit Limit : " + creditCard.GetCreditLimit());
      //           Console.WriteLine("Annual Charge :" + creditCard.GetAnnualCharge());
      //       }
      //       else
      //       {
      //           Console.Write("Invalid Card Type");
      //       }
      //       Console.ReadLine();

             // builder designPattern
            // builder report;
            // ReportDirector reportDirector = new ReportDirector();
            // // Construct and display Reports
            // PDFReport pdfReport = new PDFReport();
            // report = reportDirector.MakeReport(pdfReport);
            // report.DisplayReport();
            // Console.ReadKey();